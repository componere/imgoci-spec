# Tool versions are managed by mise.toml and mise.lock. The root project uses
# `toolchains.default: system`, so Moon orchestrates the mise-provided binaries
# without installing another language or package-manager toolchain.
